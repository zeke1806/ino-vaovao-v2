scalar Upload

type Mutation {
  login(username: String!, password: String!): Token!
  register(input: RegisterInput!): User!
  updatePhoto(file: Upload!): User!
  updateInfo(input: UpdateInfoInput!): User!
  delPhoto: User!
  sendRequest(userId: String!): User!
  acceptRequest(userId: String!): User!
  delRequest(userId: String!): Boolean
}
